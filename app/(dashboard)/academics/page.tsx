import { SectionPage } from "@/components/layout/section-page";

export default function AcademicsPage() {
  return (
    <SectionPage
      title="Academics"
      description="Timetables, subjects, class allocations, exams, results, and assignment workflows."
    />
  );
}